# See https://docs.travis-ci.com/user/languages/c/
language: c
os:
  - linux
  - osx
dist: xenial
osx_image: xcode11
# Xenial ships with GCC 5.4.0
compiler:
  - gcc
branches:
  only:
    - master
before_install:
  - if [ "$TRAVIS_OS_NAME" = "osx" ]; then brew install gcc && CC=gcc-10 && CXX=g++-10 ; fi
script:
  - make
